<div class="containar">
  <div class="row">

    <div class="col-md-3 offset-md-1 mt-3">
      <%= render 'public/users/info', user: @user %>
    </div>

    <div class="col-md-7 mt-3">
      <h4><i class="fa-solid fa-fire-flame-curved"></i> <%= @user.name %>さんフォロー一覧</h4>
      <% if @users.exists? %>
        <%= render 'public/users/user.index', users: @users %>
      <% else %>
        <p>※フォローしたユーザーはまだいません</p>
      <% end %>
    </div>

  </div>
</div>